1.
getElementById
It selects one element using its id.

2.getElementsByClassName
It selects elements using a class name.

3.querySelector
It selects the first element that matches a CSS selector.



4.querySelectorAll
It selects all elements that match a CSS selector.



<!-- number ২ -->

we create an element using

document.createElement()


<!-- number 3 -->
Event Bubbling means when we click on a child element, the event moves up to its parent.

<!--  4-->
Event Delegation means adding an event to a parent element instead of many child elements.It is useful because:

1.Less code
2.Better performance 
3.Works for new elements added later

<!-- number 5 -->
preventDefault()
Stops the browser’s default action.

and 

stopPropagation()
Stops the event from moving to parent elements.